SPÉCIFICATION DE MIGRATION UI - MH QUIZRôle : Senior Frontend Engineer / UX DesignerObjectif : Remplacer l'interface utilisateur existante par le nouveau design "Dark Mode Modern" sans altérer la logique métier (états, appels API, websockets).Stack Technique : React, Tailwind CSS, Lucide React.1. CONFIGURATION ET DÉPENDANCESL'agent doit d'abord s'assurer que l'environnement est prêt :Installer Lucide React (si absent) :npm install lucide-reactConfiguration Tailwind :Vérifier que tailwind.config.js est présent.Polices :Ajouter l'import suivant dans le fichier CSS global (index.css ou App.css) :@import url('[https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700;900&display=swap](https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700;900&display=swap)');
body { font-family: 'Montserrat', sans-serif; background-color: #030712; }
2. SYSTÈME DE DESIGN (Source of Truth)Utiliser ces composants et styles pour toutes les modifications. Ne pas inventer de nouveaux styles.Palette de Couleurs :Fond : #030712 (bg-gray-950/black)Accents : Dégradé from-orange-400 to-red-600 (Thème Host) et from-blue-400 to-cyan-600 (Thème Player).Verre (Glassmorphism) : bg-white/5 backdrop-blur-lg border border-white/10.Typographie "Puissante" :Titres : font-black tracking-tighter uppercase.Texte : font-medium tracking-wide.Composant Layout Global (À créer) :Créer un composant wrapper Layout pour gérer le fond animé.// Structure du fond
<div className="fixed inset-0 z-0 pointer-events-none">
  <div className="absolute top-0 left-0 w-full h-full bg-[radial-gradient(circle_at_50%_0%,rgba(14,28,68,0.7),transparent_70%)]"></div>
  <div className="absolute bottom-0 right-0 w-[500px] h-[500px] bg-blue-900/10 rounded-full blur-[100px]"></div>
  <div className="absolute top-1/4 left-10 w-24 h-24 bg-orange-500/10 rounded-full blur-[40px]"></div>
</div>
3. INSTRUCTIONS DE REFACTORING PAR PAGEA. Page d'Accueil (/)Action : Remplacer le contenu du composant Home.Composants UI :Hero Section avec titre "MH QUIZ" (Texte gradient).Deux grandes cartes cliquables : "Maître du Jeu" (Orange) et "Joueur" (Bleu).Icônes : <Crown /> pour Host, <Users /> pour Player.Logique à conserver :Les liens de navigation (ex: <Link to="/host"> ou Maps('/host')).B. Page Host (/host)Action : Remplacer le formulaire de création.UI Cible :Carte centrale style "Verre".Titre avec icône <Activity />.Input stylisé pour "Nom de la session".Boutons switch pour "Mode de jeu" (Normal / Speedrun).Bouton d'action large "LANCER LA SESSION" (Gradient Orange/Red).Binding de données (CRITIQUE) :Connecter l'input Nom de la session au state existant (ex: sessionName).Connecter les boutons Mode au state existant (ex: gameMode).Connecter le bouton Lancer à la fonction existante (ex: createGame()).C. Page Player (/player)Action : Remplacer le formulaire de connexion.UI Cible :Carte centrale style "Verre" (Teinte bleue).Titre avec icône <Trophy />.Input PIN : Gros caractères, centré, tracking large.Input Pseudo : Standard.Bouton "GO !" (Gradient Blue/Cyan).Binding de données (CRITIQUE) :Connecter l'input PIN au state existant.Connecter l'input Pseudo au state existant.Connecter le bouton GO à la fonction joinGame().4. RÈGLES D'EXÉCUTION POUR L'IANon-Destructif : Ne jamais supprimer une fonction logique (useState, useEffect, handlers) sauf si elle est remplacée par un équivalent direct.Tailwind Only : Ne pas créer de fichiers CSS modules ou SCSS. Tout doit être en classes utilitaires.Responsive : Vérifier que les classes md:grid-cols-2 et les tailles de texte s'adaptent au mobile.Imports : Vérifier les imports de lucide-react en haut de fichier.5. EXEMPLE DE CODE DE RÉFÉRENCE(L'IA doit se référer au code React complet fourni précédemment pour copier les classes exactes des inputs, boutons et cartes).